@model SWZ_Petrol_Station.CLIENTS

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>


@using (Html.BeginForm(new { id = Model.CLI_PKid})) {
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>CLIENTS</h4>
        <hr />
        
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.ValueFor(model => model.CLI_LOGIN)
            @Html.HiddenFor(model => model.CLI_LOGIN)
            @Html.HiddenFor(model => model.CLI_ACCTYPE)
            @Html.HiddenFor(model => model.CLI_POINTS)

        <div class="form-group">
            @Html.LabelFor(model => model.CLI_PASSWORD, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.CLI_PASSWORD, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.CLI_PASSWORD, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
